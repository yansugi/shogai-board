# 障害情報ボード 要件定義書

## 1. 概要
イントラネット上で、庁内の各システム／サービスの障害発生状況を一覧表示・更新できるWebアプリケーション。
問い合わせ対応時や職員自身が「今どのシステムに障害が起きているか」をひと目で確認できるようにすることが目的。
[部署不在ボード（Inainja）](../inainja)の姉妹アプリとして、同じ技術構成・運用方針を踏襲する。

## 2. 利用イメージ
- トップ画面は「障害情報ボード（ダッシュボード）」。現在発生中（未復旧）の障害が一覧で見える。
- 障害を発見・把握した職員が、対象システムを選んで障害情報を登録する。
- 対応が進むにつれ、対応状況（調査中→対応中→復旧済み）を更新していく。
- 障害とは別に、事前に分かっている計画的なメンテナンス（保守作業）の予定も登録・掲示できる。ダッシュボードでは障害情報と区切り線で分けて表示し、緊急のものと計画的なものを混同しないようにする。
- 権限管理は行わず、誰でも自由に登録・編集できる（緩い運用）。
- 編集・削除ボタンやナビゲーションのない閲覧専用のダッシュボード（`/Display`）も用意し、庁内モニターへの常時投影や社内ポータルへのiframe埋め込みなど、「操作させたくないが情報だけは見せたい」用途にも対応する。

## 3. 機能要件

### 3.1 障害情報ダッシュボード（トップ画面）
- 現在「発生中（未復旧）」の障害を一覧表示する。
- 一覧には、対象システム名・重要度・対応状況・障害内容・発生日時・復旧予定時刻を表示する。
- 一覧は重要度（緊急→重要→軽微）の順、同じ重要度内では発生日時が早い順に表示する。
- 対応状況が「復旧済み」に更新されたレコードは、**復旧日時から24時間**は「直近24時間に復旧した障害」として発生中の一覧とは別枠でダッシュボードに残す（復旧直後にダッシュボードから即座に消えてしまうと、見逃し確認がしづらいため）。24時間を過ぎると自動的にダッシュボードの表示からは外れる（不在ボードと異なり、物理削除はせず履歴として保持する）。
- 直近24時間に復旧した障害の一覧には、対象システム名・重要度・対応状況・障害内容・発生日時・復旧日時を表示する（復旧日時が新しい順）。発生中の一覧より視覚的に控えめなトーンで表示し、優先度が低いことを示す。
- 発生中の障害が1つもない場合は、その旨（例：「現在発生中の障害はありません」）を表示する（直近24時間の復旧済み障害があれば、そちらは別枠で引き続き表示する）。
- 発生中（未復旧）の障害が1件以上あるときは、ダッシュボード上部に赤系の警告バナー（件数・緊急件数）を表示する。直近24時間の復旧済み障害はこのバナーの件数には含めない。
- 各行に編集ボタンを設け、障害登録画面に遷移して内容（対象システム・障害内容・重要度・対応状況・時刻等）を修正できる。
- 各行に削除ボタンを設け、誤登録等で不要になった障害情報を手動で削除できる（確認ダイアログを表示してから削除。復旧済みにする場合は削除ではなく対応状況の変更で対応する）。
- 重要度に応じたバッジ（緊急＝赤系、重要＝橙系、軽微＝グレー系）、対応状況に応じたバッジ（調査中＝黄系、対応中＝青系、復旧済み＝緑系）を表示する。
- ダッシュボードは1分ごとに自動更新（再読み込み）する。

### 3.2 障害登録・編集
- 対象システムを選択し、以下の情報を入力する。
  - 障害内容（必須）
  - 重要度（緊急／重要／軽微）
  - 対応状況（調査中／対応中／復旧済み）
  - 影響範囲・対象業務（任意）
  - 発生日時（必須）
  - 復旧予定時刻（見込み、任意）
  - 復旧日時（実績。対応状況が「復旧済み」のときのみ入力対象）
- 不在ボードと異なり、障害は複数日にまたがることがあるため、発生日時・復旧予定時刻・復旧日時はいずれも日付＋時刻をまとめて1つの入力欄（日時ピッカー）で入力する。
- 復旧予定時刻・復旧日時は、発生日時より後である必要がある。
- 対応状況を「復旧済み」にして復旧日時を未入力のまま保存した場合、現在時刻を自動的に復旧日時として設定する。
- 対応状況が「復旧済み」以外の場合、復旧日時は保存しない（矛盾する状態を防ぐため）。
- 1つの対象システムに対して複数件の障害情報を並行して登録できる（不在ボードの「部署・日付で1件のみ」のような排他制約は設けない。同一システムの複合的な障害や再発にも対応するため）。
- 入力者の制限・ログインは不要（イントラ限定のため）。
- 対象システムの選択欄は全システムを対象とした検索付きコンボボックスとする（システム名で絞り込みながら選択できる）。

### 3.3 メンテナンス予定ダッシュボード表示
- トップ画面には、障害情報の一覧に続けて区切り線を挟み、「予定されているメンテナンス」欄を表示する。
- 表示対象は、まだ終了していない（予定終了日時が現在時刻以降の）メンテナンス予定。予定開始日時が早い順に表示する。
- 各行には、対象システム名・担当課・状況・メンテナンス内容・予定開始日時・予定終了日時を表示する。
- 状況（予定／実施中）は、現在時刻と予定開始・予定終了日時から自動的に判定して表示する（障害と異なり、手動でのステータス更新は不要）。
- 予定終了日時を過ぎたメンテナンスは、「予定されているメンテナンス」欄からは自動的に外れるが、**終了日時から24時間**は「直近24時間に終了したメンテナンス」として別枠に残す（障害の「復旧済み」表示と同様、終了直後にダッシュボードから消えてしまうと見逃し確認がしづらいため）。24時間を過ぎると自動的にダッシュボードの表示からは外れる（物理削除はせず履歴として保持する）。
- 障害の警告色（赤・橙）とは意図的に配色を分け、ソフトなブルー系の落ち着いたトーンで表示する（緊急対応ではなく計画的な作業であることを示すため）。
- 各行に編集ボタン・削除ボタンを設ける（操作性は障害情報の一覧と同様）。
- 閲覧専用ダッシュボード（3.7参照）にも同じ内容を表示するが、編集・削除ボタンは表示しない。

### 3.4 メンテナンス予定登録・編集
- 対象システムを選択し、以下の情報を入力する。
  - メンテナンス内容（必須）
  - 影響範囲・対象業務（任意）
  - 予定開始日時（必須）
  - 予定終了日時（必須）
- 障害と異なり、重要度・対応状況の概念は持たない（緊急対応ではなく計画的な作業のため）。状況（予定／実施中／終了）は登録時の日時から自動的に決まる。
- 予定終了日時は、予定開始日時より後である必要がある。
- 対象システムの選択欄は、障害登録画面と同じ、全システムを対象とした検索付きコンボボックスとする。
- 入力者の制限・ログインは不要（イントラ限定のため）。

### 3.5 対象システムマスター管理
- 対象システムの追加・編集・削除を画面から自由に行える管理画面を用意する。
- システムには「管轄部署」を任意で登録できる（システム選択欄等では「システム名（管轄部署）」の形式で表示する）。
- システムの削除・システム名の変更は、CSVで一括登録されたシステム（後述）についてのみ、誤操作・悪用を防ぐためマスターパスワードを知っている者のみ実行できる。各自が手動で追加したシステムは誰でも自由に削除・改名できる。
  - CSVで取り込まれたシステムのシステム名は画面上も読み取り専用にし、サーバー側でも変更リクエストを無視することで二重に保護する（管轄部署は誰でも変更可能）。
  - 削除ボタン押下時、CSVで追加されたシステムの場合のみモーダルダイアログにパスワード入力欄を表示し、サーバー側（`appsettings.json`の`SystemManagement:MasterPassword`）の設定値と一致した場合のみ削除する。一致しない場合は削除せずエラーを表示する。
  - ログイン機能を持たないイントラアプリの簡易的な抑止であり、通信経路の暗号化等の厳格なセキュリティ対策ではない点に留意する。
- システムをCSVファイルから一括登録・更新できる（管理者が多数のシステムをまとめて登録する際の手間を減らすため）。誰でも自由に追加できる手動登録と異なり、悪用・誤操作の影響が大きいため、取り込みの実行自体にもマスターパスワードが必要。
  - CSV形式：1行目はヘッダーとして読み飛ばし、1列目にシステム名（必須）、2列目に管轄部署（任意）。
  - 既存のシステム名と一致する行は管轄部署を更新し、一致しない行は新規追加する（アップサート方式）。削除はCSVからは行わない。
  - CSVから新規追加されたシステムは「CSV取込」であることを記録し、前述のとおり削除・改名にもマスターパスワードを要求する。
  - 文字コードはUTF-8（BOM有無問わず）・Shift_JIS（Excelでの既定保存形式）の両方に対応する。
  - 入力ミスの行（システム名が空、文字数超過等）はスキップし、取り込み結果（追加件数・更新件数・スキップ件数と行番号）を画面に表示する。
  - 記入例として、テンプレートCSVをダウンロードできるようにする。
- システムを削除すると、そのシステムに紐づく障害情報・メンテナンス予定（履歴含む）も併せて削除される。
- 各システムに「よく使う」チェックボックスを設ける（不在ボードと同じ仕組み）。サーバー側には保存せず、ブラウザのlocalStorageにのみ記録する。
  - 障害登録・メンテナンス登録画面の対象システム選択欄には、「よく使う」にチェックされたシステムがチップボタンとして表示され、ワンクリックで選択できる（1件もチェックされていない場合はチップ欄自体を表示しない）。
  - ダッシュボード（トップ・閲覧専用とも）では、「よく使う」が1件以上ある場合、それ以外のシステムの編集・削除ボタンを非表示にする（多数のシステムが登録された環境で、自分の管轄外のシステムを誤って操作しないようにするための絞り込み表示）。1件もチェックされていない場合は絞り込まず、従来通りすべてのシステムの編集・削除ボタンを表示する。

### 3.6 障害情報・メンテナンス予定取得API
- 現在発生中（未復旧）の障害情報一覧を取得できるJSON APIを用意する（ダッシュボードと同じ内容：対象システム名、障害内容、重要度、対応状況、影響範囲、発生日時、復旧予定時刻）。
- まだ終了していないメンテナンス予定一覧を取得できるJSON APIも用意する（対象システム名、メンテナンス内容、状況、影響範囲、予定開始日時、予定終了日時）。
- 用途：社内の他ツールやSlack/Teams等への連携。
- 認証は画面と同様になし（イントラ限定のため）。
- 例：`GET /api/incidents` → 現在発生中の障害の配列を返す。発生中の障害がなければ空配列を返す。
- 例：`GET /api/maintenances` → まだ終了していないメンテナンス予定の配列を返す。該当がなければ空配列を返す。

### 3.7 閲覧専用ダッシュボード
- 編集・削除ボタンやナビゲーションを一切持たない、閲覧専用のダッシュボード（`/Display`）を用意する。
- 表示内容はトップのダッシュボードと同じ（障害情報の警告バナー・発生中の障害・直近24時間に復旧した障害・予定されているメンテナンス・直近24時間に終了したメンテナンス）。
- 庁内モニターへの常時投影や、社内ポータルへのiframe埋め込みなど、「操作させたくないが情報だけは見せたい」用途を想定する。
- ページがiframeで埋め込まれている場合のみ、トップのダッシュボード（編集可能な方）側の登録ボタン・編集リンクを新しいタブで開くようにする（狭いiframe内で登録・編集画面がそのまま開くと窮屈になるため）。iframeでない通常表示では、これまで通り同じタブ内で遷移する。

## 4. 非機能要件
- イントラネット限定公開。認証・ログイン機能は設けない。
- 外部API連携なし。
- 実行環境：社内Linuxサーバー上で、Kestrel（ASP.NET Core内蔵サーバー）をsystemdで常時稼働させ、手前にNginxをリバースプロキシとして配置する。
- 保守・改修は依頼者本人以外に、他のIT担当者が触る可能性がある前提で、一般的でメンテしやすい構成にする。

## 5. スコープ外（今回は対応しない）
- 個人単位の担当者アサイン・作業ログ管理
- 通知機能（障害発生→復旧のアラート等）
- 障害履歴の一覧閲覧画面（DBには保持するが、専用の履歴表示画面は今回は設けない）
- システム構成図・依存関係の可視化
- 社員名簿・AD/SSO連携

## 6. 技術スタック（提案）
ASP.NET Coreはクロスプラットフォームで動作するため、Linuxサーバー上での常時稼働を前提とした構成とする。

- **バックエンド**: ASP.NET Core（Razor Pages）
  - Kestrel（ASP.NET Core内蔵のWebサーバー）でアプリ本体を実行し、systemdでサービス化して自動起動・自動再起動させる。
  - 手前にNginxをリバースプロキシとして配置し、80/443番ポートでの待ち受け・Kestrelへの転送を行う（詳細は[README.md](README.md)・[deploy/](deploy/)参照）。
- **DB**: SQLite（ファイルベース）
  - 対象システムマスター・障害情報・メンテナンス予定の3テーブル程度の小規模データで、専用DBサーバー不要。バックアップも容易。
- **フロントエンド**: Razor Pages + 素のCSS/JS
  - ダッシュボードの自動更新（定期的な再読み込み）程度は必要になる想定。
  - 障害登録画面の日時選択（カレンダー＋時刻表示・土日の色分け）のみ、軽量ライブラリ [Flatpickr](https://flatpickr.js.org/)（依存関係なし）をローカル同梱（`src/wwwroot/lib/flatpickr/`）で使用。CDNには依存せず、インターネット接続のない環境でも動作する。

## 7. データモデル（案）

### systems（対象システムマスター）
| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER PK | |
| name | TEXT | システム名 |
| owner_section | TEXT（NULL可） | 管轄部署（任意） |
| sort_order | INTEGER | 表示順 |
| is_imported | BOOLEAN | CSV取込により追加されたかどうか（削除・改名の権限判定に使用） |

### incidents（障害情報）
| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER PK | |
| system_id | INTEGER FK | 対象システム |
| description | TEXT | 障害内容 |
| severity | TEXT | 重要度（Critical＝緊急／Major＝重要／Minor＝軽微） |
| status | TEXT | 対応状況（Investigating＝調査中／InProgress＝対応中／Resolved＝復旧済み） |
| affected_scope | TEXT（NULL可） | 影響範囲・対象業務（任意） |
| occurred_at | DATETIME | 発生日時 |
| estimated_recovery_at | DATETIME（NULL可） | 復旧予定時刻（見込み） |
| resolved_at | DATETIME（NULL可） | 復旧日時（実績。ステータスがResolvedのときのみ設定） |

※不在ボードの`absences`と異なり、`(system_id)`単位での一意制約は設けない（同一システムへの複数件の並行登録を許容するため）。

### maintenances（メンテナンス予定）
| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER PK | |
| system_id | INTEGER FK | 対象システム |
| description | TEXT | メンテナンス内容 |
| affected_scope | TEXT（NULL可） | 影響範囲・対象業務（任意） |
| scheduled_start_at | DATETIME | 予定開始日時 |
| scheduled_end_at | DATETIME | 予定終了日時 |

※障害情報（`incidents`）と異なり、重要度・対応状況のカラムは持たない。状況（予定／実施中／終了）は`scheduled_start_at`・`scheduled_end_at`と現在時刻から都度算出する。
